syntax = "proto3";
package example;
service FormatData {
  rpc sample(StrObj) returns (StrObj){}
  rpc RunBenchmarkPool(StrObj) returns(ListObj){}
}

message ListObj {
  repeated string lstobj = 1;
}

message StrObj {
  string text = 1;
}